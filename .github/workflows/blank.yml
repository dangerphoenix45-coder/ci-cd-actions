name : myworkflow
on: workflow_dispatch
jobs:
  myjob:
   runs-on: ubuntu-latest
   steps:
     - name : hellostep
       run : |
        echo "first ci pipeline"
        echo "it is working"
         
