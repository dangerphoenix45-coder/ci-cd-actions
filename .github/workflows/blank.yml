name : myworkflow
on: push:
      branches:
        - main
        - test
jobs:
  build:
   runs-on: ubuntu-latest
   steps:
    - name: build
      run : echo "hello"
  test:
    runs-on: ubuntu-latest
    needs : build
    steps: 
     - name : test
       run : echo "job executed succesfully"
  deploy:
    runs-on:  ubuntu-latest
    needs: test
    steps:
     - name : deploy the app
       run : echo "deploying the app"
    
